# Current Volume — Vol-216 — OPEN (2026-06-12)

**Theme: buy the unbought multipliers; grow the exact region.**
Strict bar: 461 original (5/5 hints MANDATORY). Unguided ceiling
inherited: 452 verified, d146-basin-bounded. The 480 perspective
governs: records = search × multiplier-stack × exact-region; local
oracles are DEAD, multipliers are UNBOUGHT, growing proofs is
UNEXPLORED.

## Binding items (3)

1. **ACTUARY — Verhaard markov schedule optimizer** (named invention,
   = BACKLOG `verhaard-markov-schedule-optimizer`, absorbs
   `verhaard-order-optimization`). FITSTAT TSVs → per-depth
   fit-probability model → Markov DP over (depth, slips) predicting
   solutions-per-node for any (order, gate schedule) → offline search
   over thousands of candidates → race computed config vs hand recipes,
   ≥8 seeds min/med/max. Calibration anchor: Verhaard's released slip
   array starts at 193/256 (75%); ours 120-171/196 (61-87%).
2. **Assignment-LP prefix scoring** (kill-or-promote, one afternoon):
   HiGHS assignment LP with edge terms over the remaining pool, scored
   against the 35+ prefixes with measured finishes. PRE-REGISTERED
   PREDICTION GATE: the 451-producer (d146-seed63) must outrank its
   446-twins (d146-seed24, d146-seed36) at fixed depth, or this is a
   quadruple-null → log same-day, move on.
3. **Exact-region growth** (et20+/et28-class): last-two-rows exact
   B&B with LP-strengthened admissible bounds; loud cap-hit everywhere
   (absorbs `silent-cap-audit`). Target: move exact-tail reach from 14
   cells toward 20-28 at practical node counts; measure branching
   decades saved.

## Audit-at-open compliance

- Vol-213 aged cohort resolved (see BACKLOG vol-216 audit section):
  `ledger-color-deficit-prune` + `cairn-frontier-nogoods` →
  status-corrected to `built`; `verhaard-piece-class-quotas` /
  `verhaard-progress-restarts` → `built` (vol-214);
  `triple-break-census` + `weft-row-lb` → `wont-do` with reasons;
  `verhaard-markov-schedule-optimizer` → PICKED (binding 1);
  `verhaard-order-optimization` → absorbed into binding 1;
  `silent-cap-audit` → folded into binding 3;
  `perturb-multi-deviation` → argued, kept as standing alternate.
- Vol-215 promised vs delivered: MIDDEN program (mechanics delivered,
  economics open — `partial`), frame transfer (delivered, frame
  question CLOSED), discipline (held: 452 verified+rescored,
  honest nulls same-day).

## Standing discipline

≥8 seeds min/med/max; verify+rescore every ≥451; UTC tags; hinted
gates ≤120 or choke-derived; finals ≥300 s to read prefix quality;
never chain background runs via pgrep-on-script-name; capped exact
methods must surface cap-hit; 8 cores max TOTAL.

## Standing alternates (if bindings close early)

Learned prefix ranking (3416 banked, 30 s labels); MIDDEN temporal ×
spatial gating; k=2 witness deviations; PREFIX-VAULT consolidation.
