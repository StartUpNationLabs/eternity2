import {Piece as SolverPiece} from "../proto/solver/v1/solver.ts";

export function Piece(
    props: {
        piece: SolverPiece;
    }
    // make 4 divs for each side of the piece and rotate them accordingly. Each element of RotatedPiece reprensents a color
) {

    return (
        <div


        ></div>
    )
}