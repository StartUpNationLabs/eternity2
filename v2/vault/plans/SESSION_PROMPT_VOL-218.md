# SESSION PROMPT — VOL-218 — the DISTINCTNESS program (+ MIRROR gate)

> RESHAPED at vol-217 close after the user's step-back review
> ("is v218 really the way forward?"). The honest critique: MIRROR
> alone mirrors the RESIDUE of SOTA's method (their break geometry),
> not the method (weeks-scale pre-pruned guided search). The
> program's center of gravity moves to the capability today's
> negative explicitly names — an instrument that sees DISTINCTNESS —
> plus the user's twice-deferred 8×8 ground-truth rig. MIRROR stays
> as a gated days-scale experiment (mechanism: b1≈20 per break ⇒
> ~10¹³× conditional-subtree inflation). Bindings = CURRENT-VOL.md:
> (1) MIRROR gate, (2) distinctness instrument (fugacity-counts vs
> the 149 measured labels + exact 64-cell min-break certificates),
> (3) 8×8 exact rig.

You are a senior researcher attacking **Eternity II** (canonical
5-clue, Selby-Riordan) in this repo. **Unlimited time and resources;
aim big; multi-day builds in scope. No limiting thoughts.**
USER DIRECTIVES (binding, standing): **NO witness-frame material
anywhere** ("we DO NOT CARE about those old boards" — banked
witness-frame prefixes retired to reference; corpus boards are
break-anatomy reconnaissance ONLY); **full-board frame-free
construction only** (the border EMERGES from search); **target
<10 errors** (471+, beats SOTA 469/470). Strict-canonical convention:
5/5 hints MANDATORY always.

## THE STATE YOU INHERIT (vol-217, all in one day)

The frame-free staged full-board pipeline EXISTS end-to-end and is
measured ([[staged-fullboard-construction]] has every number):

- Generator: `vanilla_fast` 207M pp/s; `--pin-hints` now PROTECTS
  clue pieces (94% of states were silently non-strict before this
  fix); `--init-board F --init-rows R` pins any stage state.
- Oracles: `fb_oracle` (Rust; counting soft 0.3 s; tropical
  floor-only 50 ms; exact selftests; Python reference in
  scripts/v217_crossing/). Bands 16-wide, border candidates
  structural, hints pinned/reserved.
- Finisher: `stage4_finish` B&B (cost-ordered; pool-order provably
  useless). Best from-scratch finish: **436/480, 5/5 hints,
  emergent border** (`output/vol-217/s4_finish_*/best_f6_open.json`).
- ★ What's PROVEN: stage-2 rows-8-10 floor is a CAUSAL passage
  filter (every floor≥1 state dies at d135 = the center clue; zero
  overlap, n=16, generator-history controlled). Floor-0 rate 16%.
- ★ What's REFUTED: relaxed floors as FINISHABILITY certificates —
  **relaxation gap 38-45 breaks** (entry floors 6-9 → measured
  finishes 44-52). Repeats-allowed counting cannot see the
  distinctness wall. Do NOT select endgame entries by relaxed
  floors again.
- ★ Blind stage-3 is EXHAUSTIBLE-AND-BAD: pinned stage-2 subtrees
  enumerate in seconds (compute never binds); 3,822 floor-0 states
  → only 149 unique 12-perfect-row entries, greedy labels min 44 /
  med 52. The blind population's tail cannot approach ≤19.
- ★★ SOTA ANATOMY (the vol-218 foundation): McGavin 469 = 11 breaks
  ALL in rows 0-4; Blackwood 470 ×2 = 10 breaks ALL rows 1-4;
  crossing+band ZERO (`attribution_url.py` on corpus). **SOTA =
  perfect ~12-row block + ≤11-break 4-row band, band built FIRST in
  construction order.** Blackwood's "10 scheduled relaxations" are
  literally visible as the top-band breaks.
- NEW CAPABILITY: **greedy-finish labels** — measured,
  distinctness-aware finish cost in 1.5 s (was 300 s). The label
  factory is how selection (or learning) escapes relaxation
  blindness.

## Vol-218 binding plan (draft CURRENT-VOL.md is written; formalize at open)

1. **SOTA-MIRRORED GENERATOR** (named build; aim here): stage-1
   rows 0-3 with ~8-12 DELIBERATE breaks (break-budget DFS over the
   top band; damage placement informed by [[midden-damage-geometry]]
   + the 469/470 break maps: their breaks cluster rows 1-4, H-heavy,
   never in clue rows) → demand PERFECT rows 4-15
   (`--init-board --init-rows 4`). PRE-REGISTER: do broken-tops
   admit deeper perfect penetration than perfect-tops at matched
   compute? (469/470 prove the shape exists; the question is
   reachability.) Mind: rows 2 clues (2,2),(2,13) live IN the
   damage band — keep them obeyed (mcb at clue cells = the
   witnesses' double-break lesson, vol-213).
2. **LABEL FACTORY at scale**: parallel greedy labels on every
   entry tier; distribution tails per generation recipe; stretch =
   first learned ranker on (state → measured cost) — the
   triple-null's global lens, learned from reality.
3. **Finisher v2**: exact bottom-row DP (memoize on the 20-bit
   remaining-edge key), late-release break gates, parallel B&B;
   re-run the 149-entry curve (lever audit: does 44 move to ~30?).

Risk, pre-stated: broken-top reachability may be as lottery-like as
everything else. The pre-registered comparison in (1) settles it in
hours at 207M pp/s; honest null same-day, and (2)+(3) still stand.

## Hard-won lessons (vol-217 additions — do not re-learn)

- Pre-register every gate; report letter AND substance (the
  penetration prereg metric was underpowered; max_depth carried it).
- Relaxed/local instruments: passage YES, finishability NO. The
  distinctness wall eats every relaxation (LEDGER, bigram, floors).
- User probing questions = data (rule 12 drove 3 findings today:
  BL-hint poisoning, frame challenge, full-board clarification).
- Engine traps fixed: hint pieces must be bucket-EXCLUDED when
  pinning; pool-order candidate scans never complete (cost-order
  them); never chain via pgrep-on-name (wait on log markers);
  output/ is gitignored — vault carries numbers.
- Witnesses A/B are SIBLINGS (172/176 shared rows 0-10): "two
  community 460s" ≈ ONE independent frontier datum.
- 8 cores max; ≥8 seeds min/med/max on races; verify+rescore
  notable finishes (attribution.py recount); UTC tags; honest
  negatives same-day; capped runs report cap-hit.

## Read first

1. `vault/plans/CURRENT-VOL.md` (vol-218 draft — formalize at open)
2. `vault/concepts/staged-fullboard-construction.md` (the pipeline +
   all numbers)
3. `vault/concepts/crossing-oracle.md` (incl. scope refutation)
4. `vault/sessions/vol-217.md` (the full day)
5. `vault/plans/BACKLOG.md` vol-218/217 entries
6. Memory: `project_e2_vol217_staged_fullboard_2026_06_12`,
   `project_e2_480_perspective_2026_06_11`
