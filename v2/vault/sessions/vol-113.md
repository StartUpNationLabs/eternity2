# Vol-113 — CLOSED 2026-05-16 ~15:20 CEST

**Author**: autonomous agent.
**Theme**: hint-preserving blackwood-fast (T1 from vol-112's
clarification).

## T1 ✓ partial — solve_raw_with_hints

New public API `solve_raw_with_hints(puzzle, hints, time_budget_us)`
in `crates/blackwood-fast/src/lib.rs`. Internally uses a new
`solve_raw_sized_pinned<256, 256, 4>` variant of `solve_raw_sized`
with two surgical changes:

1. **At pinned depth**: hint piece is pre-placed; skip placement
   logic, just auto-advance to depth+1.
2. **On backtrack**: skip BACK through pinned cells (can't undo a
   hint).

Best-board tracking included.

New bin: `bf_bw_hinted` (10s test = 75M nps).

## Empirical verification

10s on canonical Selby-Riordan + 5 canonical hints:
- max_depth=192, nps=75M.
- Verify: **5/5 hints OBEYED**, status=OK.
- 156/256 placed (best-board: 194/256 with score 350).

30s test:
- max_depth=192, score=350, hints=5/5 OK, placed=194/256.

ALNS post-fill 3 min on the 30s hinted partial (seed=1, winning5):
- matched=415/480, **hints=5/5 OBEYED**, placed=234/256.

**The hint-preservation mechanism works end-to-end.** First
canonical-5-clue-compliant boards from the blackwood-fast pipeline.

## What was NOT done

Score is well below 459 because raw DFS (no schedule, no break-index
allowance) is structurally weaker than the v17a schedule for
canonical 5-clue solving. The vol-110/111 pipeline reached 459 via
schedule-driven `solve_blackwood_sized`, NOT raw DFS.

To get hint-preserving 459+: extend pinning to
`solve_blackwood_sized` (200-line function with schedule +
break-index + relaxed candidate list). Multi-day work.

## Vol-114 candidates

1. **solve_blackwood_sized_pinned** (extension of T1 to the
   schedule path). Multi-day careful work.
2. **Hint-preserving full pipeline**: bf_bw_hinted → bound-ascent
   (already hint-pinning-aware!) → Hungarian (needs hint pinning)
   → ALNS. The Hungarian step (`edge_target_match`) currently
   ignores hints; needs a one-line fix.
3. **Cross-check standing 459**: the vol-60 RECORD_TIE_459 has
   4/5 hints. Try running cluster_repair with just the 4 obeyed
   hints pinned (skip the 5th); can MIP find a halo-larger
   improvement?

## Standing state

- Engine: 84-85 M nps single-thread (PGO+T12+T1) for non-pinned;
  raw-with-hints: 75 M nps single-thread.
- Pipeline (hint-free): 459-460 on 0/5-hint variant.
- Pipeline (hint-pinned): 415 at this prototype level.
- Standing record (4/5 convention): 459. Unchanged.
- Strict-canonical record: 457.

## Concepts touched

- (no new concept pages; T1 mechanism doc'd in this session journal)

## Linked

- [[vol-112|vol-112 close]] (prior vol)
- [[hint-compliance-clarification]] — the methodology
  finding that motivated this work.
- [[blackwood-fast]]
