---
tags: [session, vol-212]
date: 2026-06-10
theme: "CLOISTER-II — clean `eternity2-cloister` crate (regression-gated port) + border-anchored break-DFS; strict-track target 461."
status: open
---

# Vol-212 — CLOISTER-II: border-anchored break-DFS

**Binding items** ([[CURRENT-VOL]]): (1) clean-crate refactor gated on
vol-211 numbers, (2) CLOISTER-II border-anchored break-DFS with 5/5 hints,
(3) breadth campaign (frame × seed × schedule × scan), ≥8 seeds, variance
reporting.

## Item 1 — refactor: DONE (same day)

`crates/cloister/` (`eternity2-cloister`): `model` (interior model +
candidate tables + synthetic n×n generator), `frame` (fixed ring → per-cell
rim targets), `dfs` (break-DFS, gates, hint machinery, pluggable scan,
optional LDS bound), `endgame` (exact 1-row tail + 2-row column-pair with
per-color indexing), `sa` (II+IB annealer, exact_region folds rim targets),
`border` (attach MIP), `io`/`verify`. Bins: `cloister2` (dfs/sa/hybrid/
tail2polish; `--frame <file|dir>` activates bordered mode), `attach2`.
15 tests: candidate-enumeration equivalence vs brute force, exact-tail
optimality vs brute force (both scans), hints+breaks accounting, perfect
solves on synthetic 4×4-6×6, real-board recounts.

**Regression gate (all pass)**
| check | vol-211 | crate |
|---|---|---|
| free-rim wall, 8×10 s | 173/174/174 | 172/173/174 |
| unhinted breaks5+et8, 8×30 s | 354/355/356 | **354/355/356** |
| attach Bucas-469 interior | 469 exactly | **469 exactly** (358+51+60) |
| strict-460s verify | 350+50+60, 5/5 | same |

## Honest negative — bitset candidates REFUTED (plan estimate was wrong)

The vol-212 plan predicted "bitset candidate intersection ~2-5×". Built it
(pmask per (side,color) ∧ avail word-walk, per-epoch piece relabeling for
tie-break randomness): **0.64× the vol-211 list throughput** (250M vs 390M
nodes/10 s, same config, same machine). Reason: the per-(color,color)
candidate lists ARE the precomputed intersection — deep-tree lists average
~1.5 entries, so the 4-word AND-walk per visit can't beat an indexed list
scan. Reverted to the list architecture, generalized: `pair_nw` +
**`pair_ne`** (boustrophedon) + singles + post-filters for 3rd/4th
constraints (bordered rim cells). Final ported engine: **0.94×** old
throughput with the new generality (acceptable; the old binary also carried
the refuted rim-supply tie-break, which is NOT ported).

Lesson for the vault: "intersection-precomputed lists beat bitsets when the
color alphabet is small and lists are short" — same family as the vol-16
LUT finding.

## Item 2 — CLOISTER-II (in progress)

Bordered mode: `Frame::targets` gives per-cell per-side required colors;
plans add `Fixed` constraints so IB edges enter cost/gates from cell 1;
exact tails score rim sides; `total = II + IB + BB(frame)`; record check
`≥461 & 5/5 hints` printed by the bin + `verify` module enforced before
any save (256-unique, border-legal, grey-grey never matches).

### Bordered wall + first completions (2026-06-10 ~10:00)

- **Bordered perfect wall** (II+IB all matched, no breaks, 10 s × 8): median
  **152-153/196** on all 3 control frames (strict460a/b, bucas469c) vs 174
  free-rim — the fixed ring costs ~21 cells of perfect depth. One seed
  trapped at 70 (left-bias pathology → restart/LDS motivation). Already far
  past vol-122 A1's ~116-interior-equivalent plateau.
- Default vol-211 gates (154+) are WRONG for bordered geometry (wall 153):
  0 completions. Gates 130-190 + et8: depth 183-185, still short of the
  trigger (188). **Gates 130-190 + et14 (trigger 182): 8/8 complete.**
- **First bordered completions (strict460a frame, 12 breaks, et14, 30 s ×
  8, unhinted): totals 450/451/453** (min/med/max), II 340-345, **IB
  48-50**. ★ The rim-compatibility value is COLLECTED: every vol-211
  post-hoc attach realized IB 34-39; border-anchored construction realizes
  48-50 immediately (the strict-460s themselves sit at IB=50). II is now
  the lagging term (vs 356 free-rim). Total = II+IB+60 ⇒ 461 needs
  II+IB ≥ 401; first-config 30 s gives 390-393.
- Accounting note: prefix breaks (gated, ≤ budget) + exact-tail mismatches
  (unbounded, minimized) = total breaks; the 14-cell tail currently eats
  15-18 of the 27-30 total — tail damage is the squeeze point; schedule/
  budget sweep next.

### Schedule economics + hint compatibility (30 s × 8, strict460a)

- **Tighter prefix budgets dominate at 30 s**: breaks 12 → 450/451/453;
  16 → 446/447/449; 19 → 444/445/447; 24 → 440/441/442 (totals). Looser
  budgets flood the anytime-min with sloppy completions; it can't grind
  down in 30 s. (Open: does this invert at 300 s+?)
- **Trigger-reachability arithmetic**: bordered wall 153; gain ≈ 2.5
  cells/break ⇒ ~12 breaks minimum to reach the et14 trigger (182). et8
  (trigger 188) unreachable at any tried budget — et14 is structurally
  required bordered (the S-rim targets also live in the exact tail, where
  they're optimized jointly).
- **Hinted runs (breaks 12, et14): 0 completions — depth 180-181**, one
  cell short of the trigger. The two deep hints (169, 180, both row 12)
  + reservation work; the budget is just one break short. Probes: +2
  breaks (P1) vs tail2 trigger 168 absorbing both deep hints exactly (P2).
- ★ **Frame hint-compatibility is a real constraint**: bucas469c (1-clue
  era frame) jams ALL hinted seeds at depth 13 — its top-row inward colors
  conflict with the row-1 hints' hard pre-filters. Strict-track frames
  must pass a cheap hinted 5-10 s probe (jam at <20 = incompatible).

### Hinted recipes (strict track) — first 5/5 completions (30 s × 8)

- **P1** (breaks 14, et14, gates 126-190): **1/8 completes, total 447 with
  5/5 hints** — first strict-legal CLOISTER-II board. 7/8 die at depth 180.
- **P2** (breaks 8, tail2 trigger 168, gates 120-168): **8/8 complete,
  445/445/446 (min/med/max), all 5/5.** ★ The vol-211 "tail2-in-DFS loses
  under time pressure" refutation does NOT transfer to hinted-bordered:
  the 28-cell column-pair exact tail absorbs BOTH deep hints (row 12) and
  completes reliably where 1-row et14 can't reach its trigger.
- tail2polish on the unhinted T453: **null** (tail 20 not improved at
  500M-node cap, 204 s) — prefix diversity, not tail rearrangement, is
  the lever (same as free-rim vol-211).
- Current hinted-bordered frontier: 447 one-off / 445-446 reliable;
  unhinted 453. Hint tax ≈ 6-8 at 30 s. Record needs 461 = II+IB ≥ 401.

In flight: 300 s anytime curves (P1+P2 recipes), then SA-polish probe and
the frame-breadth campaign (hint-compat probe first: 1-clue-era frames can
jam at depth 13).

## Linked
[[cloister-standalone-interior]], [[linear-placement-metric]], [[vol-211]]
