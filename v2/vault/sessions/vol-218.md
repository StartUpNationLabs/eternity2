---
tags: [session, vol-218]
date: 2026-06-12
theme: "BANDSAW — band-split exact endgame solve, validated rigorously at 10×10 (user-directed); MIRROR broken-top gate in parallel; label factory."
status: open
---

# Vol-218 — BANDSAW @ 10×10 + the MIRROR gate

**Binding items**: [[CURRENT-VOL]] — (1) SOTA-mirrored generator
(MIRROR gate), (2) band-split exact solve validated at 10×10
(BANDSAW; M0–M5 pre-registered), (3) label factory + learned ranker.

Pre-registrations (both BEFORE measurement, committed):
`plans/archive/vol-218-prereg-bandsaw-10x10.md` (~13:13 UTC),
`plans/archive/vol-218-prereg-mirror.md` (~13:55 UTC).

## Build log (with same-day catches)

- **mini.rs** (bench-audit): parametric N×N instances via
  `generate_with_solution` + 5 hints at the E2-analog cells
  ((2,2),(n-3,2),(center),(2,n-3),(n-3,n-3) in (col,row)); band
  machinery: exact distinct-piece counts, repeats-allowed counting DP
  (column-major inclusion-exclusion contractions, fb_oracle scheme at
  NC=9), exhaustive B&B, BANDSAW MITM. All brute-force selftested
  (M0): exact counts ≡ brute, relax DP ≡ repeats-allowed brute,
  bb ≡ brute min, bandsaw ≡ bb.
- ★ **M0 catch #1 — shuffled-pieces rot table**: `puzzle.pieces()`
  iteration order ≠ piece id for GENERATED puzzles (the generator
  shuffles); indexing the rotation table by iteration order scored
  the canonical solution at 30 breaks. The E2 16×16 bins never saw
  this (loader ids are sequential). Fixed: index by `p.id`.
- **Column-major band enumeration is load-bearing**: row-major
  enumeration of a free-north band is ~1e8× wider (a full free row
  before any vertical constraint binds); column-major lets the
  vertical coupling prune at every column. Same collapse the
  vol-217 program rests on, now inside the band machinery.
- **BANDSAW restructure (pre-measurement)**: bottom halves are
  enumerated PER TOP-MASK GROUP from exactly the complement pool
  (BACKLOG design (b)/(c)) instead of globally over the full pool —
  the global variant's bottom tree does not collapse (free-north,
  40-piece pool). Join = interface vectors within each group.
- ★ **Exhaustive B&B at budget b* is itself expensive at 10×10**:
  the alien-frontier M0 variant (b* high) blew through 600 s without
  exhausting — the budget-bounded tree is comparable to the relaxed
  count. Replaced with 1–2-column frontier corruptions in M0; the
  REAL exhaust-rate question is exactly what prereg M2 gates on.
- **mirror_gen** (official E2): broken-top stage-1 generator, rows
  0–4, SOTA-cell-mapped masks (469/470 attribution converted to
  cell-pays-N+W convention; 469 has a double-payer at (4,14), 470 at
  (2,10)).
- ★ **Catch #2 — the clue wall at d34**: a naive row-major DFS over
  rows 0–4 stalls for SECONDS at the (2,2) clue cell (pos 34) — DFS
  exhausts the entire subtree below an unlucky rows-0–1 prefix
  before revising the cells the clue actually depends on. Control
  arm passed only via restart luck; mask arms never. Fix:
  clue-compat pre-filter — cells directly N/W of a forced cell only
  admit candidates exposing the matching color (zero completeness
  loss). All 5 arms then bank 56 tops in 2–6 s.

## ★ M1 ANSWERED (final: all 160 grid entries, zero caps, ~14:45 UTC)

Final conditional phantom rates P(exact=0 | relax>0) — the clean
monotone statistic: k=1: 49.2% @b0 → 0% @b3; k=2: 100/100/98.4/81.8%
by b; k=3: 100% at every b; k=4: 100% where relax>0 at all. Median
log10 gap where both > 0: ~0.8 at k=1 (stable across b), 3.95 at
(k=2, b=3). Preliminary table below stands (deltas < 2pp).

## (preliminary cut, 147/160)

The relaxation-gap curve, measured exactly (relax = repeats-allowed
transfer count; exact = distinct-piece DFS count; real perfect-prefix
frontiers; b = budget ≤ 3):

| depth k | phantom rate (relax>0 ∧ exact=0) | log10 gap min/med/max (where exact>0) |
|---|---|---|
| 1 row | 20.3% @b0 → 0% @b3 | 0.78–0.90 median (×6–8) |
| 2 rows | 2.7% @b0 → **74.3% @b3** | 2.81/**3.93**/6.08 @b3 (n=25) |
| 3 rows | 1.4% → **83.0% @b3** | exact>0 NEVER (gap = ∞) |
| 4 rows | ~0 (relax floors > 3 too) | — |

- **P1.1 CONFIRMED**: median gap strictly grows with depth where
  measurable, saturating to ∞ by 3 rows.
- **P1.2 CONFIRMED** (conditional form): among relax-positive
  entries, P(exact = 0) at b=3 is 0% / 81% / 100% / 100% by depth —
  monotone. (Registered joint-rate form is non-monotone at k=4 only
  because the relax floor itself exceeds 3 there; both reported.)
- Reading: the repeats-allowed relaxation overcounts ×7 at ONE ROW
  on a 40-piece pool, ×10³–10⁴ at two rows, and is pure fiction at
  three. The vol-217 16×16 gap (floors 6-9 vs labels 44-52) now has
  a measured microscopic curve.

## ★ The exactness wall — and the LB-certificate reframe (~14:20)

Sizing runs on real blind entries (floors 4-6): BANDSAW capped 4e9
nodes INSIDE round B=6 unpruned (492 s); with tropical-suffix pruning
it completes rounds 6-7 in 94 s and still caps — **blind 10×10
entries have b\* ≥ 8 and every exact method's budget-B tree grows
~×20 per budget unit**. The vertical-coupling collapse is REAL at
cost ~0 and dissolves with damage budget — enumerability is
conditional on near-perfection (quantifies WHY the 469/470 SOTA
boards keep their damage in one band: every spent break re-inflates
the conditioned tree by b1≈20).

Reframe shipped: every COMPLETED ID round is an exact,
distinctness-aware LOWER BOUND certificate (b\* > B). BANDSAW =
exact solver within its tractable radius (b\* ≲ 7 at 10×10) + an
anytime exact-LB ladder beyond it. The relax floor cannot do this —
M1 says it is fiction precisely where this matters. M2 exactness
gate now runs on a CONTROLLED b\*-graded family (canonical-prefix
frontier corruptions, 0-6 columns) + blind entries get [LB, greedy]
brackets.

Machinery added for this: `tropical_suffix` — backward min-plus
column-state tables (backward-over-rows formulation; charges attach
to OUTPUT axes so only point reads + A/B/C/D class mins are needed);
selftests: suffix floor ≡ relax floor; suffix prune changes no
counts; prune only removes nodes.

## ★ 16×16 PORTABILITY (real stage-4 entries, ~14:38 UTC)

`mini_e2` runs the band machinery on the REAL E2 rows-12-15 endgame
(64-piece pool, row-13 hints), vol-217 entries:

1. **Cross-validation on the real board**: tropical-suffix floor ≡
   counting-profile floor on every entry tried; suffix build 1.7 s
   (the profile route took 30 s — suffix replaces it for floors).
   k=4 floors: 10-12 (vol-217's published 6-9 were k=3 rows-12-14).
2. ★ **The label factory upgrade**: column-major + suffix 1.5 s
   greedy labels BEAT stage4_finish's same-budget labels — 52 vs 53,
   52 vs 54, DNF vs DNF (n=3) — never worse. Binding 3 adopts this
   labeler (full 400-entry relabel in flight).
3. **Certified LB on the real board**: rung 10 certified (b* > 10,
   3 s); rung 11 capped at 20 s / 540M nodes. The first honest
   distinctness-aware lower bounds above the relaxation on E2 —
   but the ladder climbs ~1 rung per ×20 compute. Bracket on entry
   t00_s052: **[11, 52]**. The wall is real from BOTH sides:
   relaxations lie below it (M1), exact certification crawls above
   it (~×20/rung), and only MEASURED labels touch the achievable
   region. The "instrument that sees distinctness" exists now in
   LB form; it cannot bridge the gap — selection must learn from
   labels (binding 3), exactly as the vol-217 close conjectured.

## MIRROR gate: FLAT — honest null at registered compute (~15:25 UTC)

Race at the registered design (5 arms × 56 deduped tops × 60 s ×
1 thread, matched compute, throttle verified at 7 workers):

| arm | depth min/med/max | d≥205 tail | total med |
|---|---|---|---|
| control (perfect rows 0-4) | 201/**203**/205 | 8 | 418 |
| sota469 (11 breaks, McGavin cells) | 200/202/210 | 5 | 405 |
| sota470 (9 breaks, Blackwood cells) | 95/202/206 | 2 | 407 |
| dispersed (10, MIDDEN lattice) | 200/202/207 | 4 | 407 |
| free (budget 10, DFS-placed) | 199/202/207 | 2 | 406 |

- **P-M1 registered gate: FLAT** (deltas −1..0 vs the ±8 band). The
  b1≈20 conditional-subtree-inflation mechanism does NOT translate
  into penetration at this compute; walks stall at rows 12-13 in
  every arm and broken arms carry their pre-paid breaks as a pure
  total deficit (−11..−13 ✓ mechanical).
- P-M2: d≥192 saturated (~100% all arms — metric ceiling, declared);
  zero completions anywhere (max 210/256).
- P-M3 exploratory: the deep tail mildly favors CONTROL (8 vs 2-5 at
  d≥205); sota469 holds the single deepest top (210, n=1, noise).
- Scope honesty (per prereg): this refutes the reachability-at-60s
  claim only, not the SOTA shape (469/470 exist; they came from
  weeks-scale pipelines). Pilot at ~2 s effective (oversubscribed
  throttle bug, declared) was also flat — two compute points, same
  null. MIRROR does not earn a construction program this vol.

## ★★ 437/480 from scratch — frame-free track best (+1) (~15:15 UTC)

The 400-entry relabel with the new labeler surfaced three entries at
label 43; saved + internally rescored + **independently rescored
(rescore_board): two distinct boards at 437/480, 5/5 hints, fully
from scratch, emergent border** (entries t00_s1411, t00_s602 —
`output/vol-218/best_finish_20260612T151541/`). Deep 120 s B&B
(2.2e9 nodes) holds both at 43 — a stubborn plateau. Track history:
431 greedy → 436 (vol-217 60 s B&B) → **437 (vol-218 labeler)**.
Relabel honesty (n=101 both-labeled): new labels median −1 (60
better / 10 equal / 31 worse, deltas −15..+13) and MORE DNFs
(36 vs 22) — the suffix overhead costs nodes pre-completion; net
positive but not uniform (my earlier "never worse, n=3" was
small-sample).

## M2 FINAL: gate PASSED (all 8 instances, ~15:26 UTC)

56 ladder rungs (8 instances × b\*-graded 0..6): **mutually decided
48/48 EXACT MATCH, zero wrong answers anywhere; ID-B&B decided
56/56** (exhaust rate 100% — the prereg bar was ≥90%); BANDSAW
failed-to-decide 8 high rungs (caps, not mismatches). Registered
M2: PASS. Instrument verdict for M3: suffix-pruned ID-B&B strictly
dominates the MITM at every scale tested.

## fb-suffix-incremental: built + validated (user-prompted, ~15:45)

`tropical_prefix` (forward tables, exact 2D-exclusion mins) +
`whatif_floor` (single-column splice): per-candidate what-if floor
at 16×16 k=3 = **1.83 ms** (16× vs 30 ms full ask; k=4 65 ms =
population-reranking tier). Validated: prefix+suffix identity at
every column boundary; what-if ≡ full recompute over all
(column × color) on 5×5 ×6 bands and the real 16×16 entry. This is
the ms-ask the vol-217 row-11 decision point demanded (H3 steering).

## Hot-zone profile + the allocation fix (user-prompted, ~15:50)

macOS `sample` on a capped B&B rung: **malloc/free/realloc ≈ half
of all samples** — `enter()` allocated 3 fresh Vecs per cell entry,
millions/s. Fix: three cost passes into the retained-capacity order
buffer (zero allocs). A/B on the identical deterministic workload:
rung-10 proof 2,967 → 1,295 ms; capped rung-11 throughput 27M → 59M
nodes/s — **2.2-2.3× solver-wide** (labels, LB ladders, ground
truth). Post-fix profile: allocator gone; remaining time is genuine
search. Next lever (logged, not built): (tn,tw)-indexed per-cell
candidate buckets (vanilla_fast trick) for the scan-bound part.
Phase A ran pre-fix binary; phases B/C post-fix (per-phase
internally consistent; noted for cross-phase comparisons).

## M3 FINAL (phase B complete, 160 blind entries, ~18:05 UTC)

- **Decidability horizon**: NEITHER instrument decides a single blind
  entry's b\* (saw 0/160 at 4e9-node rounds; ID-B&B 0/160 at 120 s) —
  vs 56/56 bb decisions on the graded ladder at b\* ≤ 6. At
  10×10/8c the exact-decision horizon sits at b\* ≈ 6-7; blind
  entries live above it.
- **Certified LB on blind entries**: floors 2-7 (med 5) lift to
  LB 5-8 (med 7) — **median +2 over the relaxation** (16×16: +1 at
  cheap budgets). The distinctness instrument's honest yield.
- **MITM pathology, quantified**: final-round top enumeration up to
  57.7M raw / 35.6M keys / 6.2M mask groups — while
  **bottom_raw = 0 on every final round**: complement pools admit NO
  fillings at the remaining budget; the join never happens (9/160
  earlier-round joins gave UBs 11-14). Wall: saw med 302 s vs bb's
  120 s cap.
- **Registered deployment rule → DO NOT deploy MITM at 16×16**:
  top-key counts at 10×10 already exceed the 1e8-projection bar by
  orders of magnitude at floor+2-equivalent budgets. What deploys
  instead (and already did, validated on the real board): tropical
  suffix tables + suffix-pruned ID-B&B + LB ladder + the v2 labeler.

## In flight

- 8 instance banks generated (200 entries each, seeds 101–108,
  registered protocol).
- m23 grid (M2/M3) sizing smoke running.
- MIRROR: 280 tops banked (56 × 5 arms); race pre-registered, queued
  behind the 10×10 grid (core budget).

## Linked

[[CURRENT-VOL]], 
[[staged-fullboard-construction]], [[crossing-oracle]],
[[midden-damage-geometry]], [[vol-217]]
