# Current Volume — Vol-219 (opened 2026-06-12)

**Theme: the LABEL ECONOMY — selection learned from reality at
scale; MITM probes on the elite tail; steered generation.**
Named invention: **ASSAYER** — the first learned ranker trained on
measured greedy-finish labels (an assayer values ore by measurement,
not by appearance — the vol-218 strategic conclusion
operationalized). User target standing: <10 errors (471+). USER
DIRECTIVES standing: frame-free full-board only (border emerges);
no witness material; no 8×8 enumeration; 5/5 hints; 8 cores max.

The governing science (vol-218, pre-registered, same-day): relaxed
counts are fiction at band depth ≥2 (M1); exact certification crawls
×20/rung from below (M2/M3, horizon b\*≈6-7 at 10×10); floor-ranked
selection ≈ random (M4 P4.2) while measured greedy labels buy 3-8
breaks per cohort (M4 P4.1, 8/8); where BANDSAW's join fires (2%)
its completions beat greedy by median 12 breaks (17/17).

## Binding items (≤3)

1. **LABEL FACTORY v3 AT SCALE + ASSAYER (first learned ranker).**
   Batch labeling driver (puzzle loaded once; per entry: label-free
   features — frontier color histogram, pool composition, suffix
   floors k=3/k=4, transfer-profile covariates — plus the 1.5 s v2
   greedy label). Supply chain: stage-3 banking over ALL 3,822
   floor-0 stage-2 states (vol-217 ran only top-512 → 400 entries)
   + fresh held-out generation for validation. Train ASSAYER (GBM)
   on (features → label); **validation pre-registered BEFORE
   training**: M4 cohort-completion transplanted to the real board —
   ranker-top-K (ranked WITHOUT labels) vs label-top-K vs
   floor-top-K vs random-K, all cohorts labeled at deep budget; bars
   set at prereg after a sizing smoke. Win condition =
   generalization: ranking UNLABELED states cheaply.
2. **MITM COMPLETION PROBE at 16×16** (`mitm-completion-probe`).
   Budget-capped join probes (caps pre-registered — the M3 rule
   forbids uncapped 16×16 MITM) on (a) the 400 relabeled entries,
   (b) the factory's elite tier. Measure join-fire rate vs budget;
   harvest completions where fired (10×10 precedent: −12 breaks vs
   greedy). Concrete target: beat 437 on the frame-free track.
3. **STEERED STAGE-3 GENERATION** (fb-suffix-incremental, built,
   1.83 ms/ask): row-11 Δfloor steering at the vol-217 row-11
   decision point — population-side steered row-11 enumeration over
   banked d≥176 parents (or walker-side if cheaper). A/B steered vs
   blind entry populations on LABEL distributions at matched
   generation compute; bar set at prereg after sizing.

Alternates if bindings close early: fugacity adjoint
(forward-backward usage marginals → unlocks deferred 16×16 S1);
NEON min-plus + compact cost-0 index (quiet-machine benches only);
12×12 scaling point (M5 leftover); fb-floor-attribution as an
ASSAYER feature.

## Audit-at-open compliance (vol-216 cohort, aged 3)

Decisions recorded in BACKLOG §Vol-219 audit-at-open:
sinkhorn-lp-surrogate, sa2-tail-bound, fast-probe-generator,
strip-oracle-sat, actuary-rate-tensor-completion,
perturb-multi-deviation, endgame-friendly-frame-design → wont-do
(reasons inline); transfer-profile-features → PICKED (binding 1
feature library); staged-count-guided-construction →
resolved-as-split; mitm-completion-probe + greedy-finish-label-
factory → PICKED (bindings 2 + 1).

## Standing discipline

≥8 seeds min/med/max; prereg every gate (bars after sizing smokes,
before measurement); verify + rescore_board every notable finish;
UTC-timestamped output dirs; never mix binaries within a timed
phase; 8 cores max; honest negatives same-day; notes as you go.
