# Vol-116 — CLOSED 2026-05-16

**Theme**: T1 — hint-preserving Hungarian (`edge_target_match`), close
the strict-canonical 5/5 pipeline gap.

**Result**: SHIPPED. Full pipeline now preserves 5/5 canonical hints
end-to-end. Score ceiling on this pipeline = 435 (3-seed empirical).

## T1 ✓ — hint-preserving edge_target_match

Vol-113 T1 fixed raw-DFS to honor hints. The Hungarian step
(`edge_target_match`) still IGNORED hints and shuffled pieces freely,
breaking 5/5 compliance mid-pipeline.

### Fix

`crates/bench-audit/src/bin/edge_target_match.rs`:
- Load `hints` (was `_hints`) from `load_record`.
- Build `hint_positions` and `hint_pieces` sets.
- Exclude hint positions from per-class position pools.
- Exclude hint pieces from per-class piece pools.
- Pre-place hints on `new_board` at every iteration before matching.

3 changes, 23 insertions, 1 deletion. Compiles clean. Commit `ac54b7c`.

### End-to-end verification

Pipeline: `bf_bw_hinted 30s` → bound-ascent → Hungarian → ALNS 3min.

`verify_records.sh` at every step:

| Stage                     | matched | placed   | hints |
|---------------------------|--------:|---------:|------:|
| bf_bw_hinted partial      |     350 | 194/256  | 5/5 ✓ |
| bound-ascent output       |     222 | 194/256  | 5/5 ✓ |
| Hungarian (FIXED)         |     402 | 256/256  | 5/5 ✓ |
| ALNS 3min seed=1          |     431 | 256/256  | 5/5 ✓ |
| ALNS 3min seed=7          |     429 | 256/256  | 5/5 ✓ |
| ALNS 3min seed=42         |     435 | 256/256  | 5/5 ✓ |

**The full pipeline preserves canonical 5/5 hints end-to-end.**

## Score interpretation

Score 429-435 is well below:
- 459 (4/5 matched-edges convention record)
- 457 (strict-canonical 5/5 record, blackwood_mrv)

Reason: the raw-DFS bf input (no schedule, no break-index) is
structurally weaker than schedule-driven bf. The Hungarian + ALNS
recovery can compensate for ~30 points of seed weakness but not for
the ~110-point gap between raw-DFS and a strong canonical-aware seed.

This is consistent with vol-15 findings: schedule + break-index drive
the bulk of Blackwood's lift; the engine speedup (232×) doesn't change
the algorithmic ceiling.

## What it unblocks

Vol-117+ candidate: extend hint-preservation into
`solve_blackwood_sized` (schedule + break-index path). ~1-2 days
careful work (BLACKWOOD_RAW unsoundness under break-index allowance
makes this non-trivial). Would let us re-attack strict-canonical 457
record with the new fast engine.

## Standing state

- Engine: 84-85M nps unchanged.
- Hint-preserving pipeline ceiling: **435 / 480** strict-canonical 5/5.
- Standing record (4/5 convention): **459** unchanged.
- Strict-canonical 5/5 record: **457** unchanged.

## Linked

- [[vol-115|vol-115 close]]
- [[../concepts/hint-compliance-clarification]]
- vol-113 raw DFS commit (hint-preserving raw variant)
- commit `ac54b7c` — vol-116 T1 hint-preserving Hungarian
