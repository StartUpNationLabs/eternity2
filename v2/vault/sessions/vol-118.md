# Vol-118 — IN-PROGRESS 2026-05-16

**Theme**: Critical bf bug discovered, fixed, infrastructure consolidated.
Hint-preserving schedule path UNBLOCKED; strict-canonical pipeline
ceiling honest at 446 (was contaminated 452).

## Tasks shipped (in order)

### T1 ✓ — rigidity matrix → two-cluster 459-level-set confirmation

`scripts/vol118_rigidity_matrix.py` pairwise σ-cycle analysis on
7-board 459 corpus reveals binary structure:
- Cluster A: 5 distinct basins, Hamming 34-44, max σ-cycle 9-25.
- Cluster B: 1 basin (vol-60 RECORD_TIE), Hamming 251-253, max
  σ-cycle 94-190.

Sharpens conjecture C1. Concept: [[../concepts/459-level-set-two-cluster-confirmed]].

### T2 ✓ — pipeline corner-perm specificity

Running our pipeline on McGavin's corner perm (p22) reaches only 396
strict-canonical. Confirms vol-68 — our pipeline is basin-specific.
Concept: [[../concepts/pipeline-corner-perm-specificity]].

### T3 ✓ — σ-cycle subset score-lift theorem

`MATH_NOTES_2026-05-16_SIGMA_SUBSET_THEOREM.md` documents:
Δ(S) ≈ -B(S)·p with p≈0.92-1.0 empirically. Greedy min-boundary 35-45%
better than contiguous, but isoperimetric gap ~2.5×. σ-subset attack
formally bounded.

### T4 ✗ — intra-cluster σ-subset bridge REFUTED

Even within Cluster A (smallest σ-cycles 4-25), partial cycle
application + ALNS plateau ≤ 459 across 15 trials. Concept:
[[../concepts/intra-cluster-sigma-subset-refuted]].

### T5 ✓ — hint-pin conflict-propagation FIX

Critical bug in vol-117 T1 `solve_blackwood_sized_pinned`: pinned cells
propagated forced mismatches into `conf[depth]`, causing depth-35
wedge with v17a schedule. Fix: carry conf forward unchanged at pinned
cells.

Empirical: v17a + pin reaches depth 192 (was 35), score 338, 5/5
hints OK. Concept: [[../concepts/hint-pin-conflict-propagation-fix]].

### T5b ✓ — parallel hint-preserving bf

`solve_blackwood_par_with_hints`: 8-thread bf with shared pin state,
per-thread bucket shuffle. Reaches depth 232 in 60s (vs single
thread's 192). Concept: [[../concepts/parallel-hint-preserving-bf]].

### CRITICAL FIX — bf candidate-bucket bug

User-discovered (visual inspection of bucas render): the bf engine
placed 8 edge-pieces at interior cells with BORDER edges facing
inward at positions 208-228. Root cause: `crates/blackwood-fast/src/lib.rs:233`
pushed every (piece, rotation) into tbl=0 bucket unconditionally.

Fix: a piece-rotation now goes into EXACTLY ONE bucket per
(right_is_border, bottom_is_border) pair. Same fix applied to
`build_relaxed_index`. Concept: [[../concepts/bf-candidate-bucket-bug]].

**Impact**: existing complete-board records (459, 469) not contaminated
(complete-board ALNS removes illegal placements during optimization).
bf PARTIAL outputs since vol-15 were potentially affected.

### Post-fix honest strict-canonical ceiling

Pre-fix sweep (8 trials): max 452, median 449.
**Post-fix sweep (8 trials): max 446, median 443.5.**

6-point drop attributed to bug giving ALNS denser initial seeding via
8 illegal-anchor cells. `basic` beats `winning5` 4/4 in paired
comparisons. Concept: [[../concepts/strict-canonical-pipeline-honest-446]].

### T7 ✓ — canonical verifier

`eternity2_export::verify` + `verify_board` bin replace
`rescore_board` + `verify_record`. Checks piece-uniqueness +
border-consistency (catches the bf-bucket bug class) + hint-compliance
+ edge score. 4 unit tests pass.

### T8 ✓ — canonical board I/O

`eternity2_export::load_board` reads all 3 in-the-wild formats
(DumpedBoard, placement-indexed, placement-sparse). `save_board` /
`save_board_csv` write the canonical placement-sparse format. 3
unit tests pass. Plus `docs/BOARD_FORMAT.md` documenting the schema.

### T9 (in progress) — migrate per-bin load_board duplicates

2/41 bins migrated (verify_board, print_bucas). 39 remain.
Reduces line count + risk surface.

### T6 (running, restart) — UB-filter par-bf sweep with fixed engine

8 seed-offsets × par-8 bf 60s + bound-ascent UB filter; ALNS only on
UB ≥ 458 partials. So far seed-offset 0: UB=458, ALNS max 446
(consistent with general saturation finding).

### T10 (queued) — bf → Hungarian direct (skip bound-ascent)

Hypothesis: bound-ascent homogenizes color profiles → all paths land
in UB=458 basin. Skipping it might preserve diversity.

## Open at close (likely)

- T6 UB-filter sweep completes: probably ~446 ceiling across all seeds.
- T10 experiment: untested whether skipping bound-ascent helps.
- T9 migration: 39 bins still have duplicate load_board.
- Honest strict-canonical pipeline ceiling: 446. Gap to record 457: 11 points.
- Standing record 459 (4/5 convention): unchanged.

## Linked

- [[vol-117|vol-117 close]]
- All concepts above
- [[../MATH_NOTES_2026-05-16_SIGMA_SUBSET_THEOREM]]
- [[../PAPER_2026-05-16_459_indecomposability_synthesis]]
