---
tags: [session, vol-217]
date: 2026-06-12
theme: "The CROSSING program: frontier-conditioned crossing oracle built + gate PASSED decisively (witness floors 3-4 vs banked 7-8); crossing-ranked selection; toward crossing-guided construction."
status: open
---

# Vol-217 — the CROSSING program

**Binding items**: [[CURRENT-VOL]] — (1) CROSSING ORACLE,
(2) crossing-ranked selection, (3) crossing-guided construction.

## Audit-at-open (2026-06-12)

Vol-216 cohort fresh (no aged items). Status corrections in BACKLOG:
`assignment-lp-prefix-scoring` → built; `actuary` → partial;
`exact-region-growth` → partial. Restored lost `sinkhorn-lp-surrogate`
header (text had merged into the staged-count entry). New entries:
`crossing-oracle` (binding 1), `crossing-guided-construction`
(binding 3).

## Binding 1 — CROSSING ORACLE (built + GATE PASSED, ~10:00 UTC)

Setup data first (attribution.py, run BEFORE prereg, disclosed in it):
realized breaks on the crossing 87-edge set — witnesses 7-8/20
(ALL at interior 11→12 V + row-12 H; zero in rows 10-11.5), our
452: 14/28 and 451s: 11/29 (spread from the frontier down; the 452
pays 4 breaks on N-of-row-10 alone). Witnesses cross the first half
of the wall PERFECTLY.

Preregistration 09:58 UTC (`output/vol-217/crossing_oracle_20260612T095808/
preregistration.txt`): instrument fixed, primary gate = witness
floors ≤ min trio floor − 2, numeric prediction witness 3-4 vs
banked 7-11; secondary = ρ > 0.335 on the n≈75 set; sanity A/B for
the repeats relaxation.

Build: 3-row cell-by-cell transfer with marginal match/mismatch
contractions; **exact brute-force selftest PASS** (2-col × 3-row
reduced instance, all 15 cost classes equal to exhaustive count,
forced-cell path exercised). 0.8 s/prefix.

★★ **GATE PASSED DECISIVELY**: witness A floor **3** / soft 15.20,
witness B floor **4** / soft 14.87 vs trio floors 7/8/8, softs
9.5-11.1. Prediction hit exactly. First static instrument that sees
the witnesses as special ([[band-oracle]] had scored witness A BELOW
our 452's pool). Crossing cost is to first order a property of the
frontier rows 8-10 present → statically measurable, steerable during
construction. Full table: [[crossing-oracle]].

## Secondary gate + floor census (~10:30 UTC)

Population (n=81 homogeneous ≥300s, all instruments same set):
registered ρ_max bar FAILED (0.270 vs LP 0.365) — but ρ vs MEDIAN =
**0.381, best ever** (band 0.284, LP 0.192); crossing↔band residual
correlation only 0.220 (band↔LP 0.627): a genuinely NEW factor.
Sanity A/B: repeats relaxation benign (trio order preserved).

★ **Floor census: the bank has ZERO witness-grade frontiers** —
255 labeled prefixes: floors 6-11 (mode 9), one 6, none ≤5;
witnesses 3-4 sit OUTSIDE the entire bank distribution. The 452
ceiling, mechanistically: d14x probes produce witness-grade
crossings at rate <0.4%. The instrument is a between-class selector
+ construction objective, not a within-class regressor. Bars:
floor ≤5 interesting / ≤4 witness-grade / ≤3 461-grade.

d139 pre-cell extension shipped (1,666 more scoreable; selftest +
seed63 regression green).

## User session (mid-day): frame challenge + FULL-BOARD pivot

User probing questions (treated as data, per discipline):

1. **"Why are we using that frame?"** Honest answer: historical
   default since vol-212/214 (`ladder.sh` FRAME default), never
   revisited; 68% of the bank is strict460a-framed by inertia. The
   frame is EXONERATED for the 452→460 gap by the strongest control
   (witness A reached 460 inside strict460a; we reach 452 in the
   same frame) and by the n=55 framegen census (universal 444-450
   band). BUT three real exposures stand: (a) every ceiling we own
   is conditional on a 3-frame family; (b) the 480 board's ring is
   almost surely none of ours — hard-fixing a ring excludes the
   target; (c) no frame was ever chosen on merit → new BACKLOG
   lever `endgame-friendly-frame-design`. **USER DIRECTIVE
   (binding): no new material on witness frames; banked
   witness-frame prefixes are reconnaissance only.**
2. **Staged 4-row solver clarified as FULL-BOARD**: the user's
   staged proposal means the real 16×16, all 256 pieces, frame-free
   — border emerges (top edge stage 1, flanks per band, bottom edge
   LAST against the surviving pool). Witness anatomy in this
   staging: stages 1-3 PERFECT, all 20 breaks in stage 4. Binding 3
   re-scoped accordingly (CURRENT-VOL updated): fb_oracle (16-wide
   mixed-pool), frame-free stage-1/2 generator, stage-3 population
   builder, stage-4 min-break endgame. The crossing rim-targets
   become DECISION VARIABLES — the frame objection and the staging
   merge into one architecture.

## Binding 2 — full-bank census + races (launched ~13:00 UTC)

Full-bank sweep (3,617 unique d139+ prefixes, 7 workers): floor
distribution d140+ : one 5, ~40 6s, mode 8-9; d139 (incl. pre-cell
offset): mode 8. **The bank's best frontier is floor 5 — a
singleton** (`prefix_d143_strict460a_seed66`, soft 13.81, the
highest soft seen). Nothing witness-grade (3-4) in ~3,000 scored.
Top-12 (floor≤6 by soft) racing at rung-3 config (native frames,
threads 7): `output/vol-217/race_topk_*`.

## Binding 3 — fb_oracle built + ★ the row-11 decision point (~14:00 UTC)

`fb_oracle.py`: full-board N-row frame-free oracle (border emerges
from remaining border pieces; structural sides; exact selftest PASS).
Validation findings ([[crossing-oracle]] full table):
1. ★ **The crossing floor is decided at the row-11 entry cells**:
   seed24's rows-0-10 frontier is floor-3 (witness-grade!) but its 6
   row-11 commits destroyed it (→8); witnesses preserved (3→3/4);
   seed63 never had one (6→7). Crossing quality = frontier quality ×
   commitment quality, both measurable, the latter steerable.
2. ★ **Witnesses A/B are SIBLINGS**: 172/176 identical cells in rows
   0-10 (one construction, two endgames). Witness evidence = ~1
   independent frontier. (Diff-before-narrate rule honored.)
3. Cross-check: T452 rows-0-10 ≡ seed63 (same basin).

User green-light: build the full staged pipeline ("alright let's go
for it"). Rust oracle port justified (ms asks for row-11 steering).

## Stage-2 exit ask: anchors (smoke, ~10:50 UTC)

vanilla_fast IS the frame-free stage generator (full-board row-major,
hints enforced in-walk, on-visit snapshots; indexed-JSON format
support added to fb_oracle loader). 1-thread 20 s smoke: 64 states.
Rows-8-10 ask (bmax 8) anchors: **witness stage-2 floor 0 (soft
21.6), our 452 floor 0 (soft 22.9!), random smoke states floors 1-3**
— even repeats-allowed counting sees the wall at stage-2 exit; random
states are often doomed before row 8; good constructions keep
floor 0. (452's stage-2 soft EXCEEDS witness — consistent with
vol-216: the early board is not where the gap is.) The stage-2→3
filter is real; floor-0 rate at scale = first census question.

## ~10:45 UTC: USER DIRECTIVE — 460 borders DEAD; full research mode

"STOP using the borders from the 460, we DO NOT CARE about those old
boards." Consequences executed immediately: top-12 race batch KILLED
mid-flight (2/12 had completed; not analyzed — witness-frame material
is dead); seed24 row-11 rebuild dropped (witness frame); the banked
prefix corpus (68% witness-framed) retires to reference status.
Witness boards remain ONLY as already-logged validation anchors. The
program is now 100% frame-free staged full-board construction.

Rust fb_oracle port: built (`crates/bench-audit/src/bin/fb_oracle.rs`,
batch mode + rayon), selftest EXACT, cross-validates vs Python to 4
decimals on all anchors; 1.3 s → 0.3 s/ask after fused-kernel pass.
Python file = reference spec.

## Vol-217 research program (hypotheses, frame-free track)

- **H1 (stage-2 filter strength)**: floor-0 rate at the rows-8-10 ask
  among random frame-free stage-2 states is low (smoke 0/4; controls
  floor-0). Census → rate + soft spread among floor-0 survivors.
- **H2 (boundary independence)**: stage-2-ask quality does NOT
  predict crossing quality (area-law prior; vol-216 one-latent-factor
  lesson) ⇒ filter sequentially at each boundary, never greedily once.
- **H3 (row-11 steering)**: greedy Δfloor-minimizing construction of
  the stage-3 exit rows preserves frontier floors (seed24 +5 vs
  witness +0 says commitments dominate). Needs ms asks.
- **H4 (floor mechanism)**: crossing/stage floor ≈ color-supply
  deficiency between frontier-demanded colors and pool sides (a
  Hall-type certificate). If true: interpretable, µs-cheap, and gives
  the builder a CONSTRUCTIVE rule (exit with souths in abundant
  colors). Test: perturb frontier colors, correlate floor vs supply.
- **H5 (border-partition lever, NEW and unique to frame-free)**: the
  60 border pieces partition exactly 14 top + 28 flank + 14 bottom +
  4 corners. Stage 1 chooses the top 14 ⇒ chooses the complement
  available for flanks/bottom. The bottom-row 1D chain profile over
  remaining edges (µs DP) at stage-2 exit = an early, cheap,
  steerable signal NOBODY has ever optimized (frames froze it).
- **H6 (oracle perf path)**: tropical (min-plus) floor-only DP — no
  b-axis, ~15× less work + early abort ⇒ filter rung at ~ms;
  suffix-incremental recompute for per-candidate Δfloor; f32; bmax
  trim per ask.
- **H7 (state abstraction)**: (frontier souths, pool bitset) is the
  COMPLETE lower-board state — banked files are mere reachability
  witnesses. Census dedup/clustering on this key; the staged search
  is a search over (frontier, pool) pairs.
- **H8 (diversity economics)**: on-visit snapshots within one thread
  share deep prefixes; measure piece-overlap, tune restart/offset
  policy (vol-35 offset sweep precedent).

## H4 first probe: honest negative (~10:58 UTC)

Bigram certificate VACUOUS: zero infeasible frontier bigrams on ALL
anchors (floor-3 witness and floor-8 trio alike; min serving pairs
1-2 everywhere; `bigram_probe.py`). One-row local necessary
conditions are again empty (LEDGER lesson) — the floor lives in
≥2-row joint chaining, irreducibly the DP's object. H4 refined →
**floor-attribution**: extend the oracle to report WHICH edge
classes carry the b_min breaks (argmin backtrace through the DP) —
interpretable steering data. BACKLOG.

## Tooling shipped for the staged pipeline (~11:00 UTC)

- `fb_oracle.rs` + `--bottom-chain` (H5 instrument: row-15 1D chain
  profile over remaining border pool, µs).
- `vanilla_fast --init-board F --init-rows R`: pin a stage state,
  DFS on top, backtrack floor at the pin boundary → the stage-3
  population builder.
- `census_driver.py`: dedup snapshots by the H7 state key before
  asking; multiplicity = H8 diversity data.

## ★ Hint-piece poisoning caught by USER QUESTION (~11:05 UTC)

Generation 1: 124e9 placements/10 min (207M pp/s), 10,461 snapshots,
ALL unique by the H7 state key (H8 answered: diversity is free at
this engine speed + 333 ms spacing). But the user asked "why is the
BL hint not pinned?" on a sample Bucas URL → scan revealed **94%
(1,882/2,000) of stage-2 states consumed a clue piece off-hint in
rows 0-7** — unfixably non-strict. Mechanism: `--pin-hints` enforces
clue CELLS but never protected clue PIECES from consumption
elsewhere (self-correcting for deep solves; poison for shallow
banking). The vol-35 pin_hints bug class in a new costume, caught
day-one by rule 12 (user probing question = data). FIX:
`reserved_pid` exclusion of hint pieces from all normal buckets when
pinning (vanilla_fast bucket build); hint cells already use the
separate hint pool. Census-1 demoted to PILOT (shape only, nothing
promoted); clean regeneration follows.

## Perf pass (user-directed, ~11:10-11:15 UTC)

- Counting mode: fused IE kernels + per-step offset tables:
  1.3 s (Python) → 0.55 s (naive Rust) → **0.30 s/ask**.
- **Tropical (min-plus) floor-only mode** (H6): no b-axis, exact 2D
  exclusion via per-(q, w0) row-min tables — a greedy top-k skyline
  was REJECTED as inexact (adversarial entries sharing e1's column
  escape it; caught pre-ship). Selftest: tropical floor == counting
  bmin; anchors match (452→6, 451→6, smoke→2). **46-70 ms loaded**
  (8 busy cores), expect ~25-40 ms unloaded.
- Architecture consequence: census-2 is TWO-PASS (tropical filter on
  all → counting+bottom-chain on floor≤1 + strata samples) ⇒ same
  census budget covers a 2× longer generation (20 min, ~20k states).
- Discipline catch: first census-2 chain waited on pgrep-by-name
  (forbidden); re-armed on the generator's own gen.log completion
  marker.
- Deferred (logged, not built): per-candidate suffix-incremental
  asks (~ms steering), f32 counting, counting-mode (n,w)-major
  locality pass.

## ★ CENSUS-2 (clean): H1 ANSWERED (~11:50 UTC)

23,774 unique frame-free stage-2 states (20 min × 8 threads; all
unique by state key — diversity holds at scale). Tropical rows-8-10
floor distribution: **floor 0: 16.1% | 1: 14.5% | 2: 38.0% |
3: 29.1% | >3: 2.3%**. Both quality controls (witness + our-452
stage-2 states) were floor-0 ⇒ good constructions live in the 16%
tail; the 35 ms tropical filter kills 84% before any expensive work.
Soft strongly floor-stratified (f0 top 24.6 / f0 bottom 20.7 /
f1 20.2 / f2 19.3). Strata-16 picked per prereg; penetration
experiment IN FLIGHT (`output/vol-217/penetration_20260612T115332`).

## ★★ PENETRATION EXPERIMENT (~12:11 UTC): the staged filter is REAL

Results (16 strata states × 60 s × 8 threads, pinned rows 0-7):

| stratum | max depth | d≥192 banked |
|---|---|---|
| f0_top | 184/**195**/182/190 | 0/**8**/0/0 |
| f0_bot | 143/136/143/159 | 0 |
| f1_mid | 135×4 | 0 |
| f2plus | 135×4 | 0 |

1. **Zero overlap f0 vs f≥1, and the cut is CAUSAL**: every floor≥1
   state died at exactly d135 = cell (8,7) — THE CENTER CLUE (their
   frontiers cannot legally pass it); the f0_bot states share the
   SAME generator history (d130-135) as f1/f2 yet passed (136-159).
   The oracle adds information beyond generator reachability.
2. Soft predicts depth within floor-0 (top 182-195 vs bot 136-159,
   no overlap).
3. HONEST scorecard vs prereg: P1/P2 registered on median n_d192 —
   0 vs 0 tie at 60 s (underpowered metric); separation carried by
   max_depth (recorded secondary). P3 passes as registered. Letter:
   partial; substance: unambiguous (n=16, zero overlap, predicted
   direction).
4. First trophies: **8 perfect frame-free rows-0-11 states** from
   t01_s3048 (soft 24.55) — the program's first stage-3 completions.
5. Mechanism note: the stage-2 floor is empirically a "can you pass
   the center clue + row 8" certificate — the clue cell is the
   binding constraint at the band's entry (constraint-immediacy made
   visible).

## Stage-3/4 first generation (12:30-12:50 UTC)

Top-512 f0 × 30 s: 135 d≥192 snapshots from 74 producing states
(14.5% yield), best depth 200; → 75 unique perfect rows-0-11 states.
Stage-4-entry (rows 12-14) floors: **6×1, 7×20, 8×32, 9×20, 10-11×2**
(median 8); bottom-chain floor 0 with soft 13.7-14 at the top (H5:
emergent borders do NOT starve the bottom edge). Reading: blind
stage-3 = mediocre exits (SEMAPHORE lesson at a new boundary);
stage-3 must be SELECTED/steered on its exit ask.

## ★★ SOTA BREAK ANATOMY (user target <10; reconnaissance, ~12:55)

`attribution_url.py` on corpus encodings:
**McGavin 469 (5-clue canonical): 11 breaks, ALL in rows 0-4 —
crossing 0, band 0. Blackwood 470 ×2: 10 breaks, ALL rows 1-4.**
SOTA = perfect ~12-row block + ~10-break 4-row band — the user's
staged architecture MIRRORED. Blackwood's "10 scheduled relaxations"
are literally the 10 top-band breaks. Existence proof for
"≤10-11 breaks confined to a 4-row band" (and the clue layout is
180°-symmetric, so the geometry transfers in principle).
Consequences:
1. User target: <10 errors, all in the LAST 4 rows. Bound side says
   entry floors ≤4 needed (current blind entries 6-11); overnight
   hunt = all 3,822 f0 states × 30 s + chained stage-4 census
   (~7k-entry tail sample), in flight.
2. Reality side: stage-4 finisher (break-budget DFS, 64 cells,
   iterative deepening) = next build; measures the entry-floor →
   achieved-total curve on real entries.
3. SOTA-anatomy fallback (feasibility-proven shape): budget moved to
   STAGE 1 (~10 scheduled breaks rows 0-3) + perfect rows 4-15;
   pipeline supports it unchanged (asks are break-tolerant).

## ★ HONEST NEGATIVE + the quantified relaxation gap (~13:20 UTC)

stage4_finish (B&B, cost-ordered after pool-order scanning measured
ZERO completions in 500M nodes): the floor-6 entry finishes greedy
at **49 breaks (431)**, B&B 60 s → **44 (436)**. Greedy labels on
ALL 75 first-gen entries: **median 51, p25 47** breaks (one 1.5-s
label DNF). ⇒ **The relaxed stage-4-entry floor (6-9) does NOT
certify finishability — relaxation gap ≈ 38-45 breaks.** The
vol-209 distinctness wall, quantified at this exact boundary.
Blind-banked frame-free entries are 430-class vs the interior
track's 452 and SOTA 469/470. The relaxation-oracle SELECTION
approach, as constituted, is REFUTED for the endgame boundary
(it remains REAL for wall-passage: the penetration result stands).

What this buys (the pivot the data proposes):
1. **Greedy-finish labels at ~1.5 s** are a NEW capability: measured,
   distinctness-aware entry quality at label-factory rates (the
   triple-null's "global lens" can be SELECTED-ON or LEARNED from
   reality instead of derived from relaxations — millions of labeled
   (state, finish-cost) pairs per night were impossible before).
2. **SOTA-mirrored budget placement**: 469/470 prove the
   perfect-12-block + 10-break-band SHAPE — with the band built
   FIRST in construction order (their breaks: rows 0-4). Variant to
   test: stage-1 with ~10 oracle-placed breaks, then perfect rows
   4-15 demanded (perfect-walk engines at 207M pp/s are our
   strength). Honest context: SOTA came from weeks-scale compute on
   mature pipelines; day-one 436 is not the architecture's verdict,
   but the entry-selection instrument question is now open.

## Open at this point

- Overnight: stage-3 banking (3,822 × 30 s ≈ 4 h) → stage-4 census →
  greedy-label the full entry crop (replaces relaxed-floor ranking).
- Vol decision tomorrow: labels-pivot vs SOTA-mirrored variant
  (likely both: labels rank, mirrored shape generates).
- fb-floor-attribution, suffix-incremental: hold until the entry
  instrument question settles.
- H1 answer (floor-0 rate), H5 spread, H8 multiplicity from census.
- Then: pick top stage-2 states → init-board stage-3 populations →
  stage-3-exit ask (rows 12-14 + chain) — the full staged loop, day 1.
- Tropical floor-only mode (H6) before steering work.

## Linked

[[crossing-oracle]], [[band-oracle]],
[[assignment-lp-prefix-scoring]], [[CURRENT-VOL]], [[vol-216]]
