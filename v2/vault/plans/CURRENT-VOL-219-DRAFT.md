# Current Volume — Vol-219 — DRAFT (prepared at vol-218 close, 2026-06-12)

**Theme: the LABEL ECONOMY — selection learned from reality at
scale, MITM probes on the elite tail.** Vol-218 settled the
instrument question with pre-registered rigor: relaxations are
fiction at band depth ≥2 (M1), exact certification crawls at
×20/break from both sides (M2/M3), floors ≈ random for endgame
selection while measured greedy labels buy 3-8 breaks per cohort
(M4, 8/8). The only instruments touching the achievable region are
MEASURED: greedy labels (v2: column-major + suffix, beats v1) and —
the M4 discovery — MITM completion probes, which on the 2% of
entries where their join fires complete a median 12 breaks better
than greedy (17/17). User target standing: <10 errors. USER
DIRECTIVES standing: frame-free full-board only; no witness
material; no 8×8 enumeration.

## Binding items (≤3)

1. **LABEL FACTORY v2 AT SCALE + the first learned ranker**
   (carries vol-218 binding 3, now with v2 labeler + 2.2× B&B):
   overnight labeling across stage-2/3 tiers and generation recipes
   on the real 16×16; train the first ranker (features: frontier
   colors, pool composition, suffix floors as covariates — labels as
   target); validate by cohort-completion (the M4 protocol, now on
   the real board).
2. **MITM COMPLETION PROBE at 16×16** (`mitm-completion-probe`,
   vol-218 M4 discovery): budget-capped join probes on
   label-selected entries — measure the 16×16 join-fire rate; where
   it fires, harvest the completions (10×10: −12 breaks vs greedy).
   If the fire rate is nonzero on good entries, this is the
   cheapest finisher upgrade available; compose with the 437-track.
3. **STEERED STAGE-3 GENERATION** (fb-suffix-incremental now built,
   1.83 ms/ask at k=3): vanilla_fast-side or population-side row-11
   Δfloor steering toward low-crossing-floor frontiers (the vol-217
   row-11 decision point, finally actionable); A/B steered vs blind
   entry populations on label distributions.

Alternates: fugacity adjoint (the 16×16 S1 gate needs it — finite
differences measured infeasible); NEON min-plus vectorization +
compact cost-0 index (quiet-machine benches); 12×12 scaling point
(M5 leftover).

## Standing discipline

≥8 seeds min/med/max; prereg every gate; verify+rescore every
notable finish; UTC tags; 8 cores max; honest negatives same-day;
notes as you go.
