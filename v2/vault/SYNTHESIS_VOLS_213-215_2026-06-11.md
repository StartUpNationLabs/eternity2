# SYNTHESIS — Vols 213–215 (2026-06-10 → 2026-06-11, one continuous ~34 h session)

**Read this + `REMINDER_USER_DIRECTIVES.md` + `plans/CURRENT-VOL.md`
before any new work. Strict bar unchanged: 461 original board, 5/5
hints. This synthesis supersedes nothing — it consolidates three
volumes for fresh-context pickup.**

## Records & verified boards (all independently rescored, 5/5 hints)

| score | provenance | file |
|---|---|---|
| **452** | unguided high — LADDER prefix d146-seed63 + 1 h finish, II 339 + **IB 53** (highest non-witness IB) | `output/vol-215/incumbent6h/../T452_*seed8.json` (first found in `incumbent3600`) |
| 451 | the band-breaker (LADDER run-1); basin-typical yield | `output/vol-214/ladder_*/r3_prefix_d146*/T451_*.json` |
| **460 ×2** | exact reconstructions of BOTH community strict-460s (REPLAY+mcb2+et-cap) — engine validation, not new records | `output/vol-213/cloister2_dfs_20260610T1713*/T460_*.json` |
| 458 | witness-derived tie of the all-time record, 3rd basin | vol-213 REPLAY mcb=1 |

All-time strict record for ORIGINAL boards: still 458 (vol-122).

## The five inventions of the session

1. **REPLAY (prior-over-cost)** + **double-break (mcb=2)** [vol-213,
   [[replay-prior-over-cost]]]: witness boards exactly walkable. ★★ The
   community 460s contain 4-5 DOUBLE-BREAK cells (one cell paying 2
   mismatches) — provably unreachable for violate-≤1 engines. Their
   1-deviation neighborhoods are EMPTY across [60:182) (~5M completions).
2. **framegen** [[framegen-chain-feasibility]]: chain-feasible ring
   generation, 73× hint-compatible yield. Frame question CLOSED:
   generated frames fully competitive (gen0143 → 450); band 444-450
   universal over 55+ frames; witness rings hold no magic.
3. **Choke instrument + auto-gates** (`--schedule-from-choke`): hinted
   wall measured at 139 (not 153); auto-gates moved et14 reach 146→180,
   first hinted-et14 completion. LESSON (cost 3 batches): hinted gates
   must start ≤120; `--breaks` default spread is unhinted-only.
4. **LADDER (prefix racing)** [vol-214, [[ladder-prefix-racing]]]:
   user's idea; broke the band on run 1 (451), extended to 452.
   Characterized to death: prefix-specific (finishes deterministic
   given prefix, need ≥300 s to read); EMPIRICAL racing is the only
   valid promoter; budget laws (~3 cells/break completability; quality
   needs tight 12-14, pins at depth−15); capital must accumulate;
   escape rate ~1/1400 probes; d146 basin FLAT at 6 h (451/452 ceiling).
5. **MIDDEN (damage-geometry)** [vol-215, [[midden-damage-geometry]]]:
   first-ever cell-set (WHERE) damage control vs everyone's depth
   (WHEN) gates. Dispersed lattices extend the perfect wall 153→174
   (+21); graded-density design rule (follow the choke gradient);
   **completion economics UNSOLVED** (budget drains pre-trigger;
   temporal × spatial gating is the open design).

## The central negative result (and its precise scope)

**Triple-null on prefix-quality signals**: the 451-producer is
indistinguishable from 446-twins by (a) aggregate pool statistics
(LEDGER deficit ≡ 0 on ALL perfect prefixes — vacuous; tilability =
depth proxy), (b) placement neighborhoods (10% median overlap — every
draw isolated), (c) FITSTAT local dynamics (yield/death curves
identical). Prefix quality is an emergent GLOBAL property — direct
empirical confirmation of [[isentrope-entropy-growth]] distinctness.

**Scope**: this kills cheap/local/static scoring ONLY. Honest framing
(user-corrected): the community 460s came from Blackwood-class
backtracking AT SCALE (months × machines vs our hours × 8 cores) —
brute force with an engineered multiplier stack (computed orders ×5,
slip schedules, piece quotas, smart restarts). No known result
demonstrates a non-search shortcut, and the area-law theory predicts
none exists locally. **Records = search × multiplier-stack ×
exact-region.** Our gap is in factors 2-3 plus raw node-hours. The
untested multipliers: Verhaard markov optimizer (input collected),
global assignment-LP prefix scores, learned ranking (~1700 banked
prefixes, 30 s labels, vol-26/27 GNN precedent). The only lever that
changes practical complexity rather than constants: GROW THE EXACT
REGION (et14 → et28-class with stronger bounds) — proof, not luck.

## Engine additions (all tested, 24+4 green; crate `eternity2-cloister`)

`--prior-over-cost`, `--max-cell-breaks 2`, `--et-cap` (+ loud cap-hit
warn — the TAIL_CAP lesson: a silently-capped exact method is greedy in
disguise; cost 2 invisible breaks until a pre-registered prediction
caught it), `--replay-perturb`, `--ledger` (+E2_LEDGER_CHECK validator),
`--cairn`, `--abort-below`, `--save-prefix`, `--init-prefix`,
`--quota` (built, marginal), `--break-cells` (MIDDEN), `--scan spiral`
(REFUTED: closure-tax mechanism), FITSTAT TSVs, E2_TRACE.
Harnesses: `scripts/v213_replay/*`, `scripts/v214_ladder/*`
(ladder.sh FRAME/seed0-parametrized; ladder_rank2.py — NOTE the fixed
ring-pollution bug: frame JSONs are FULL boards, filter ring positions).

## Dead ends with mechanisms (don't redo)

Spiral-in (free-rim k≤1 branching / bordered closure-tax); CLIMB
single-corridor re-basing; static deficit/tilability scoring;
unguided mcb=2 (both lanes); 22-break loose schedules (flood to 442);
flat lottery beyond ~1400 probes; per-cell tilability quotas at first
config (marginal); 1-deviation witness perturbation (complete, empty).
Plus 3 chaining/harness bugs documented in [[vol-214]]/[[vol-215]].

## The agenda handed to the next session (priority order)

1. **Verhaard markov optimizer** (`verhaard-markov-schedule-optimizer`):
   fit-probability model → computed order + gates. Input collected.
   The single most-credentialed unbuilt idea (his records used it).
2. **Global prefix scoring**: assignment-LP with edge terms over
   remaining pool (196−d × 196−d, milliseconds via HiGHS) — the lens
   the triple-null never tested and the theory endorses.
3. **Learned prefix ranking**: generate (prefix, 30 s finish) labels at
   scale; small model; vol-26/27 precedent.
4. **MIDDEN economics**: temporal × spatial gating (gate the dispersed
   region, keep the tail open).
5. **k=2 witness deviations** (`perturb-multi-deviation`): the guided
   track's only remaining enumeration.
6. PREFIX-VAULT consolidation (~1700 banked prefixes across
   output/vol-214/*, output/vol-215/*).

## Sessions: [[vol-213]], [[vol-214]], [[vol-215]] (full journals).
