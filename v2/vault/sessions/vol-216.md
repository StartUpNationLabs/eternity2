---
tags: [session, vol-216]
date: 2026-06-12
theme: "Buy the unbought multipliers: LP prefix scoring + band oracle validated (the global lenses); ACTUARY calibrated; the 452→460 gap LOCALIZED to the wall crossing."
status: closed
---

# Vol-216 — multipliers + exact region

**Binding items**: [[CURRENT-VOL]] — (1) ACTUARY (Verhaard markov
schedule optimizer), (2) assignment-LP prefix scoring, (3) exact-region
growth (et20+).

## Audit-at-open (2026-06-12)

Vol-213 aged cohort resolved in BACKLOG: `ledger`/`cairn`/`quota`/
`abort-below` status-corrected to `built` (engine flags shipped in
vols 213-214 but BACKLOG lagged); `triple-break-census` + `weft-row-lb`
→ wont-do (reasons in BACKLOG); markov optimizer PICKED as binding 1;
order-optimization absorbed into it; `silent-cap-audit` folded into
binding 3; `perturb-multi-deviation` argued and kept as alternate.

## Binding 2 — assignment-LP prefix scoring (FIRST RESULT, ~07:10 UTC)

★★ **THE PRE-REGISTERED GATE PASSES.** LP relaxation of the
max-matched-edges completion (x[cell,piece,rot] assignment polytope +
per-edge color-mass match variables y[e,k] ≤ endpoint mass, HiGHS):

| prefix (d146, strict460a) | measured finish | LP_obj | fixed |
|---|---|---|---|
| seed63 (the 451-producer) | **451** (≥300 s, many runs) | **104.245** | 302 |
| seed24 (446-twin) | 446-447 (60 s; 30 s races) | 102.142 | 302 |
| seed36 (446-twin) | 446-447 (60 s; 30 s races) | 102.568 | 302 |

- Fixed term identical (302 = 135 H + 132 V + 35 rim, hand-verified —
  perfect-walk bookkeeping confirmed): the separation is PURE LP.
- Margin +1.7 to +2.1 LP-points over the twins. UB sound (466.2 ≥ 451).
- **The triple-null does NOT extend to the global lens**: prefix
  quality is invisible to aggregate stats / neighborhoods / local
  dynamics (vol-215), but the assignment-LP — which couples the entire
  remaining pool through the assignment polytope + edge terms — SEES it.
  Exactly what [[isentrope-entropy-growth]] predicts: the signal is
  global distinctness, and this is a global relaxation.

### Validation complete (same morning)

1. **Empirical control (twins at 300 s × 8, identical run-1 rung-3
   config)**: seed24 → 446/447/448; seed36 → 446/447/448 (n=7, one
   seed died). seed63's 450/451/451 stands alone. The LP ordering
   (104.2 > 102.6 > 102.1) matches the empirical ordering exactly.
2. **Population-level (all 257 banked prefixes scored, 0.85 s each
   via HiGHS IPM)**: vs 30-s rung-2 labels: NULL (median within-group
   ρ ≈ 0.01 — but those labels are known-noisy; the star itself reads
   447 at short budgets). vs the calibrated ≥300-s label set
   (n=75 homogeneous ladder.sh config): **ρ(depth-residualized UB,
   max-finish) = 0.335 (p ≈ 0.003)**, consistent across all 3 frames
   (0.42/0.30/0.28); the star ranks **#2/75**; top-quartile enrichment
   for max≥450 prefixes ≈ 2× base rate.
3. Depth trend: ub_total ≈ 509.6 − 0.309·depth (R² low, slope real);
   cross-depth comparison REQUIRES residualization.

**Verdict: signal CONFIRMED, strength = pre-filter grade, not oracle
grade.** The first static lens to survive contact with prefix quality
(after the triple-null). Adoption: LADDER rung-0 = LP pre-rank, race
top-k empirically (successive halving gets a free static rung).
NOT a replacement for empirical racing.

Score definition (for the concept page): lp_obj = LP optimum of the
assignment+edge-match relaxation over remaining (cells × pieces);
ub_total = fixed + lp_obj + 60 (sound UB on total); promoter score =
ub_total residualized on depth (pooled slope −0.309/cell).

## Binding 1 — ACTUARY (mid-day): model calibrated to 5%

Engine: cost-split FITSTAT (`y0/y1/starts/starts_open`) + `fit2`
(d, spent)-conditional block with y2 lane. Concept page
[[actuary-markov-optimizer]] has the full error ladder — headline:

- **Telescoping lemma**: depth-marginal branching rates reproduce the
  calibration run identically and carry ZERO schedule signal (the
  naive DP overestimated arrivals ×2.3e5). The (d, s) split is the
  transferable content. Three further mechanisms (y2 hint-cell lane,
  lane-coherent fallback, forced-break lanes) close self-consistency
  to **8.99 predicted vs 8.55 measured arrivals/epoch (5%)**.
- **Gate multiplier measured**: b1 ≈ 20-22 children per gate-open
  arrival vs b0 ≈ 1 — every gate is a ×20 tree multiplier; schedules
  trade wall-survival against node explosion.
- Anneal (γ=1.0, coverage ≥ 0.9): computed schedule
  `134,138,147,153,155,158,164,167,169,170,173,177,179,182` —
  spend-lean epochs (32 nodes/epoch vs hand 4.6e7), predicted
  ~300×/node arrival efficiency (~59× after pin-walk overhead).
  RACE in flight (300 s × 8, pre-registered in
  `output/vol-216/actuary_race1_*/preregistration.txt`).

## Binding 3 — exact-region growth (day 1 engineering)

`exact_tail`: k>14 made safe (the old fixed `[_;56]` candidate array
would overflow at et20 — latent bug found by reading), value-based
admissible LB (true min-cost per cell vs the old 0/1 sat bound),
Hungarian assignment root cut (O(k³), forced/reserved/MIDDEN aware).
Exactness verified vs brute force at k=4/6; k=20 smoke green.
Wall-clock A/B et14 vs et18/et20 queued. [[exact-region-growth]].

## Q&A session log (user present, ~09:00-12:00 UTC) — ideas + live results

The user drove a long Q&A; several live experiments ran inside it.
Everything below is banked (concept pages / BACKLOG / here).

**Strategy (user-endorsed, keep front of mind)**: from-scratch
construction, with multipliers that make each unit of from-scratch
search smarter — brute construction at our scale tops out at ~452
without them. Existing boards are reconnaissance (what optima look
like), never material. The user explicitly confirmed this framing.

**Live results produced during Q&A:**
1. **TL→TR hint-chain count** (transfer matrix, exact):
   **2,100,166,303,731,037 ≈ 2.1e15** E-W-valid 10-cell chains between
   the top two hints; ONE row of vertical coupling collapses it to
   ~1e3 — fifteen orders of magnitude: the area law made concrete.
   1D objects are free; ALL constraint mass is 2D stitching.
2. **Perfect bottom-row counts = 0** for all three d146 pools — the
   452-class boards' bottom-row breaks are FORCED, not found
   (BACKLOG `transfer-profile-features`).
3. **Break-tolerant row profiles rank the star**: seed63 has 2× the
   b=3 closure supply of seed24; seed36's floor is b=4. First
   MICROSECOND-scale feature to rank the 451-producer top. λ1 of the
   pool transfer matrix ANTI-correlates (good pool = lower chain
   entropy = specificity over abundance; n=3).
4. **[[piece-tensor-spectroscopy]]**: the canonical piece 4-tensor is
   anomalously FLAT (eff. rank 16.96 > random p95; leading SVs below
   random p5) — low-rank color embeddings are foreclosed BY DESIGN.
5. **[[fugacity-corrected-counts]]**: equal-case distinctness
   correction VALIDATED vs ground truth (err ≤0.002 log10 vs naive
   0.78 at 15 cells and growing). Subset case needs a conditioned
   term (documented negative). Path to honest log-#completions(prefix)
   — the theoretically-correct prefix score.

**Measured answers given:**
- Time-to-depth (hinted+framed perfect walk): d60 ≈ ms; d139-140 ≈ 1 s
  (modal); d146+ ≈ 8% of 5-s probes (~8 s wall on 8 cores); >153 never.
- Engine speeds: cloister2 ~15M n/s/thread; blackwood-fast 79M (PGO);
  vanilla_fast 125M pp/s; solver-engine CSP ~0.1-1M (smart/node).
- Two-front DFS: seam scan measured −3..−5 (vol-213; IB 55 record as
  side effect); LIGHTHOUSE merge refuted; mechanisms = shared-pool
  coupling + closure tax + walls-are-pool-starvation-not-positional.
- Hint-link paths: 51/480 vs row-major 433 (vol-36) — corridors are
  k=1 (untested choices), pockets are k≥3 (late refutation);
  the engine gets hint linkage LOGICALLY via req back-propagation.
  Synthesized as [[constraint-immediacy-principle]].

**New BACKLOG entries from this Q&A**: `fast-probe-generator`,
`strip-oracle-sat`, `transfer-profile-features`,
`sinkhorn-lp-surrogate`, `sa2-tail-bound`,
`actuary-rate-tensor-completion`, `8x8-exact-counting-rig`,
`staged-count-guided-construction`.

## Band-oracle PoC (user-directed, built + VALIDATED same session)

[[band-oracle]] — the inter-stage oracle from the user's staged-
construction proposal, instantiated at the band-4 boundary (coupled
2-row break profile, hints pinned, all edges break-tolerant,
1.4 s/prefix). Pre-registered gate PASSED decisively (seed63 soft
5.82 / floor 7 vs twins 1.34-2.46 / floors 8-9); population
ρ(resid,max)=0.300 ≈ LP's 0.335, and **ρ(resid, MEDIAN)=0.297 — 3×
the LP's median signal** (reads typical finish, not lottery tail);
star #2/75 combined. Band↔LP correlation 0.644 ⇒ one latent
pool-health factor, two lenses; more static lenses = diminishing
returns; next grade needs honest counts (fugacity-MPS) or racing.
THIRD validated global instrument of the day. Adopted as LADDER
rung-0 candidate alongside the LP.

## ★★ THE WITNESS-POOL EXPERIMENT — the 452→460 gap localized

Scored the community 460s' own d146 pools with the band oracle:
**witness endgame pools are ORDINARY** (A: soft 3.09 — below our
452's 5.82) and their realized bottom-band breaks EQUAL ours (16-17).
Witnesses pay 3-4 breaks outside the band; we pay ~11; both prefix
classes perfect to 146. ⇒ **the entire 8-break gap lives in the
wall-crossing rows ~10.5-12.** Not the endgame. Not the early board.
This (a) explains the ρ≈0.3 ceiling of all static instruments built
today (they measure the wrong rows for the dominant variance),
(b) defines the next instrument — the CROSSING ORACLE (rows 10-12,
prefix-conditioned N boundary, pre-registered gate: witness floor
3-4 vs banked 8-11), (c) names the innovation path: **CROSSING-GUIDED
CONSTRUCTION** — select/construct prefixes by crossing cost, spend
MIDDEN/gates where the gap actually is. Full detail: [[band-oracle]]
witness section.

## Vol close (2026-06-12, one day, user present most of it)

Binding scorecard: (1) ACTUARY — model calibrated to 5%, race-1
honest negative with mechanisms, iteration 2 (quality histogram +
pooled refit) handed to vol-217 — `partial`. (2) LP prefix scoring —
**built + validated + adopted** (`built`). (3) Exact-region — k>14 +
value-LB + Hungarian shipped; et18/20 in-DFS NET NEGATIVE at current
bound strength; next rung = SA2/SDP — `partial`.
Unplanned deliverables: [[band-oracle]] (third validated instrument,
best median signal), [[fugacity-corrected-counts]] (equal case
validated), [[piece-tensor-spectroscopy]], 
[[constraint-immediacy-principle]], and ★★ the witness-pool
localization (the 452→460 gap = wall-crossing rows 10.5-12) — the
vol-217 headline. 8 commits; 8 new BACKLOG entries; all pre-registered
gates honored (5 pre-registrations, 3 passed, 2 honest negatives).

## Linked

[[assignment-lp-prefix-scoring]], [[actuary-markov-optimizer]],
[[exact-region-growth]], [[band-oracle]],
[[fugacity-corrected-counts]], [[piece-tensor-spectroscopy]],
[[constraint-immediacy-principle]], [[ladder-prefix-racing]],
[[CURRENT-VOL]], [[vol-215]]
