# Vol-107 — CLOSED 2026-05-16 ~11:40 CEST

**Author**: autonomous agent.
**Theme**: extend vol-106's blackwood-fast engine with per-schedule
const-table monomorphisation + basin-feature analysis.

## Audit-at-open

See [[../plans/CURRENT-VOL|CURRENT-VOL.md]] for the full disposition
of 23 unbuilt BACKLOG items. Summary: score-axis deferred for vols
106-115 window; solver-engine perf wont-do (bypassed by
blackwood-fast); tech-debt deferred; RL out of scope.

## Binding items

### T1 ✓ — Per-schedule v17a const-table monomorphisation

Shipped. `solve_blackwood_unrolled_256_v17a_const` uses
`TARGETS_V17A_CANONICAL: [u32; 256]` and `CONFLICTS_V17A_CANONICAL:
[u32; 256]` const tables (computed at compile time via `const fn`)
so the per-D schedule/conflict lookups fold to immediate values.

Single-thread canonical Selby-Riordan, 10s × 3 runs (apple-m1):

| variant            | nps (M) | Δ baseline |
|--------------------|--------:|-----------:|
| baseline           |    63   |       —    |
| + PGO              |    72   |     +14%   |
| + T12 unrolled     |    76   |     +21%   |
| + T12 + PGO        |    79   |     +25%   |
| + T1 const         |    82   |     +30%   |
| **+ T1 const + PGO** | **84-85** | **+35%** |

Opt-in: `E2_BF_UNROLLED_V17A_CONST=1`. Same trajectory; correctness
verified across runs.

### T2 partial — Basin-vs-ALNS-liftability via σ-cycle decomposition

Initial N=4 finding suggested max-σ-cycle to vol-60's 459 record
predicts ALNS lift. Extended to N=6 with 3 more ALNS measurements:
**HYPOTHESIS WEAKENED**.

| offset | max_cyc | ALNS 60s |
|-------:|--------:|---------:|
|      0 |      29 |   (450)  |
|     50 |  **23** |  **444** | smallest cyc, low lift
|    100 |      71 |     446  |
|    200 |      44 |     448  |
|   1000 |      31 |     447  |
|   2000 |      29 |     450  |

Partial conclusion: max-cycle > 50 IS a negative signal (offset=100
case validates), but max-cycle < 30 doesn't guarantee good lift.
The predictor is too simple. Vol-108 candidate: richer basin
features.

Concept page: [[../concepts/sigma-cycle-predicts-alns]].

### T3 ✓ — `scripts/build_release.sh` shipped

One-command PGO build for bf_bw + vanilla_v2 + bf_similarity.
Verified throughputs after the script: bf_bw 78M nps,
vanilla_v2 97M pp/s. Closes T3.

## What was refuted

(via the T2 follow-up): the simple "max σ-cycle predicts ALNS-lift"
hypothesis. Documented as PARTIAL — one direction holds (large cycle
is bad), one doesn't (small cycle isn't always good).

## Concepts touched

- [[../concepts/blackwood-fast]] — engine reference. T1 measurements
  added to its variant table.
- [[../concepts/rust-perf-at-scale]] — perf log. T1 const-table win
  added to the cumulative table.
- [[../concepts/sigma-cycle-predicts-alns]] — NEW vol-107 concept,
  status `partial`.

## Open at close

Vol-107 CLOSED with all 3 binding items shipped or partial:
- T1 ✓ Per-schedule const tables (+7% on T12, +35% total).
- T2 partial — σ-cycle predictor weakened at N=6.
- T3 ✓ build_release.sh one-command PGO.

Vol-108 candidates (carry forward):
- T2 follow-up: extend to N=16 partials with richer basin features.
- Rich-feature ALNS-liftability predictor.
- σ-cycle-aware ALNS destroy op (custom op targeting cells in the
  largest σ-cycle to a known good basin).
- Cross-machine bf_bw benchmark.
- BOLT post-link reordering on apple-m1.

## Linked

- [[../INDEX]]
- [[../plans/CURRENT-VOL|vol-107 plan]]
- [[vol-106|vol-106 close]] (prior vol)
- [[../concepts/blackwood-fast]]
- [[../concepts/rust-perf-at-scale]]
- [[../concepts/sigma-cycle-predicts-alns]]
